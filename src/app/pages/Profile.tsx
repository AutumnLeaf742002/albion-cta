import { AppLayout } from "../layout/AppLayout"

import { Text } from '../../ui/components';

export const Profile = () => {

    return (
        <AppLayout>
            <Text>
                Perfil
            </Text>
        </AppLayout>
    )
}