import { AppLayout } from "../layout/AppLayout"
import { Text } from '../../ui/components/Text';

export const Builds = () => {
    return (
        <AppLayout>
            <Text>
                Builds
            </Text>
        </AppLayout>
    )
}